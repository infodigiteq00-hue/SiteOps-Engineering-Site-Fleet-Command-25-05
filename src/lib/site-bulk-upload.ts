/**
 * Create-new-site bulk upload uses the same 6-column machinery template as Add machinery.
 * @see machinery-bulk-upload.ts
 */
export {
  MACHINERY_BULK_SAMPLE_CSV as SITE_BULK_SAMPLE_CSV,
  MACHINERY_BULK_CSV_HEADER as SITE_BULK_CSV_HEADER,
} from "@/lib/machinery-bulk-upload";
